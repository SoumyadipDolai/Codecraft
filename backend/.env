# Environment Variables
PORT=3000
NODE_ENV=development

# Database (Using SQLite for easy local dev - no setup needed)
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET=healthvault-dev-secret-key-2024
JWT_EXPIRES_IN=7d

# Email (Dev mode - OTPs will be logged to console)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Frontend URL
FRONTEND_URL=http://localhost:8081
